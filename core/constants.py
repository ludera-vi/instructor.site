"""
Общие константы приложения.
"""
from datetime import time

# Радиус блокировки вокруг занятого слота (симметричный, минуты).
# Если клиент записан на T, то слоты в диапазоне (T−90, T) и (T, T+90) закрываются.
BLOCK_RADIUS_MINUTES = 90  # 1 ч 30 мин

# Времена для быстрого добавления слота в ЛК (каждые 15 мин, 06:00–20:45)
QUICK_SLOT_TIMES = [
    time(h, m)
    for h in range(6, 21)
    for m in (0, 15, 30, 45)
]
